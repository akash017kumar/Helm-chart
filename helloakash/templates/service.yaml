apiVersion: v1
kind: Service
metadata:
  name: {{ include "helloakash.fullname" . }}
  labels:
    {{- include "helloakash.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "helloakash.selectorLabels" . | nindent 4 }}
